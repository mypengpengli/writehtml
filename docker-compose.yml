services:
  write:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # 数据库放在挂载卷里，容器重建不丢稿
      - DB_PATH=/data/writehtml.db
    volumes:
      - ./data:/data
