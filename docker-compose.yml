services:
  write:
    build: .
    restart: unless-stopped
    ports:
      - "9123:9123"   # 改端口就改这两个数（左=宿主机，右=容器）
    env_file:
      - .env
    environment:
      # 数据库放在挂载卷里，容器重建不丢稿
      - DB_PATH=/data/writehtml.db
    volumes:
      - ./data:/data
